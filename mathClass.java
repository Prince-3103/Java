public class mathClass {
    public static void main(String[] args){

        // System.out.print(Math.PI);

        double temp;

        // temp = Math.pow(2,3);    // Power
        // temp = Math.abs(-4);        // Negative to Positive
        // temp = Math.sqrt(49);     // Square root
        // temp = Math.round(2.49);    // Closest integer value
        // temp = Math.ceil(2.19);     // Always give top value
        // temp = Math.floor(2.89);    // Always give down integer
        // temp = Math.max(10,15.34);     // Maximum value between 2 number
        temp = Math.min(10, 20);    // Minimum between 2 number
        
        System.out.println(temp);

    }
}
